/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package io.github.loustler.skeleton.app;

import io.github.loustler.skeleton.list.LinkedList;

import static io.github.loustler.skeleton.utilities.StringUtils.join;
import static io.github.loustler.skeleton.utilities.StringUtils.split;
import static io.github.loustler.skeleton.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
